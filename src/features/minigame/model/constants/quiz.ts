export const QUIZ_ROUNDS = 3;
export const QUIZ_OK_THRESHOLD = 2;
export const QUIZ_PICK_DELAY_MS = 350;

export const QUIZ_PHASE = {
  READY: 'ready',
  LOADING: 'loading',
  PLAYING: 'playing',
  RESULT: 'result',
} as const;
