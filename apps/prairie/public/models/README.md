# 모델

- `runner.glb` — 말. Quaternius LowPoly Animated Animals(CC0). 뼈·클립 이름은 `src/entities/runner/model/constants`에서 참조한다(Head 뼈에 뿔을 붙이고, Idle/Gallop 클립을 섞는다).

잔디 blade는 glb 없이 절차 생성한다(`src/entities/grass/model/constants`의 `makeBladeGeometry`). 다른 오브젝트도 전부 프로시저럴이다.
