name: Weekly Webnovel

on:
  schedule:
    - cron: '0 2 * * 1' # 매주 월요일 오전 11시 KST (UTC 02:00)
    - cron: '25 9 30 4 *' # 임시: 2026-04-30 09:25 UTC 1회 실행
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main

      - name: Set timezone to KST
        run: |
          sudo timedatectl set-timezone Asia/Seoul
          echo "Current time: $(date)"

      - name: Get current date
        id: date
        run: echo "date=$(date +'%Y-%m-%d')" >> "$GITHUB_OUTPUT"

      - name: Setup Git
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Python dependencies
        run: pip install google-genai playwright

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-${{ hashFiles('**/weekly-webnovel.yml') }}

      - name: Install Playwright browsers
        run: playwright install chromium --with-deps

      - name: Fetch webnovel data
        run: python scripts/fetch_webnovel.py ${{ steps.date.outputs.date }}
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

      - name: Create Pull Request
        id: cpr
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: '✨ feat(${{ steps.date.outputs.date }}): 주간 웹소설 베스트셀러 추가'
          title: '✨ feat(${{ steps.date.outputs.date }}): 주간 웹소설 베스트셀러 추가'
          body: |
            ## 📋 변경사항
            - 날짜: ${{ steps.date.outputs.date }}
            - 파일: `contents/book/${{ steps.date.outputs.date }}-webnovel/index.md`
            - 카카오페이지·네이버 시리즈·리디 웹소설 베스트셀러 수집 및 Gemini AI 분석
          branch: 'feature/${{ steps.date.outputs.date }}-webnovel'
          base: 'main'

      - name: Auto-merge Pull Request
        if: steps.cpr.outputs.pull-request-operation == 'created'
        run: |
          for i in 1 2 3 4 5; do
            gh pr merge "${{ steps.cpr.outputs.pull-request-number }}" \
              --squash --auto --delete-branch && break || sleep 10
          done
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
